const Card = ({ random, suit, value }) => {
    const style = {
        position: "absolute",
        borderRadius: "3px",
        transform: "rotate",
    };

    return <div> Post </div>;
};

export default Card;
